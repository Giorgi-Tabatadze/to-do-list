import { pubsub } from "./pubsub";

const mainList = {
  parent: null,

  render: (list) => {
    
  },

  setParent : (container) => {
    mainList.parent = container;
  }
}

export default mainList;